<div class="container brewery-container">
  <p id="notice"><%= notice %></p>

  <h1><%= current_user.username %>'s Brewpubs</h1>

  <table id="accounts-table" class="display table table-bordered table-striped" data-source="<%= account_path(format: :json) %>">
    <thead>
      <tr>
        <th>Name</th>
        <th>City</th>
        <th>State</th>
        <th>Date Visited</th>
        <th></th>
      </tr>
    </thead>

    <tbody>
    </tbody>
  </table>

  <br>

  <%= link_to 'New Brewery', new_brewery_path, method: :get %>
</div>
